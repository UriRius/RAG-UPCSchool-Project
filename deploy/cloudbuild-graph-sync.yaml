steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/$PROJECT_ID/cosora-graph-sync', '-f', 'deploy/Dockerfile.graph-sync', '.']
images:
- 'gcr.io/$PROJECT_ID/cosora-graph-sync'

options:
  logging: CLOUD_LOGGING_ONLY
